# Pinned to the environment the proximity service was run and tested in (Python 3.12.1, Windows, CPU).
# Install into its own venv: python -m venv vision/.venv && vision/.venv/Scripts/pip install -r vision/requirements.txt
ultralytics==8.4.160
torch==2.14.0
torchvision==0.29.0
opencv-python==5.0.0.93
# mediapipe depends on opencv-contrib-python; pin it to the same build so both ship one identical cv2
opencv-contrib-python==5.0.0.93
numpy==2.5.3
lap==0.5.13          # ByteTrack assignment; ultralytics would otherwise try to pip-install it at runtime
httpx==0.28.1
# Fatigue (models.md §5). 0.10.21 was the last release with the legacy solutions.face_mesh API and
# needs numpy<2, so we use the Tasks FaceLandmarker (same 478-point mesh) from 1.x.
mediapipe==1.0.1
# dev
pytest==9.1.1
ruff==0.16.8
black==26.5.1
# Not installed yet; pin when used.
python-dotenv~=1.0
