VITE_SUPABASE_URL=https://<ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon>
VITE_API_URL=http://localhost:8000
VITE_WS_URL=ws://localhost:8000
# true (default): screens run on web/src/mocks/*.json (scripts/build_mocks.py) and a mock WebSocket.
# false: Supabase (anon key + RLS) and the FastAPI backend above.
VITE_USE_MOCKS=true
# Live mode: data time when no replay is running (picks the operator's shift at sign-in). The demo
# window: Ganesh (OP02) lands on SH-2026-08-19-M05-N. Leave empty to use the wall clock.
VITE_DATA_NOW=2026-08-19T15:15:00Z
